# uds-rdma-proxy
uds-rdma-proxy

This repo contains a prototype of a unix domain socket (UDS) to RDMA proxy.

The idea is to allow any program that supports UDS to be tunneled over RDMA ( most likely ROCEv2 ).
